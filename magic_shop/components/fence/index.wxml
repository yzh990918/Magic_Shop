<view class="container">
  <view class="title">{{fence.title}}</view>
  <view class="cell-container">
    <block wx:for="{{fence.Cells}}" wx:key="{{index}}">
    <cell class="cell" Cell="{{item}}"></cell>
    </block>
  </view>
  <view class="hr"></view>
</view>
