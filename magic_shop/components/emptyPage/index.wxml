<!-- 通用空页面组件 -->
<view class="container">
<image class="img" src="../../imgs/imgs/empty.png" />
<text class="text">{{text}}</text>
<l-button wx:if="{{showBtn}}" size="mini" bind:lintap="onTap" l-class="btn">{{btnText}}</l-button>
</view>
